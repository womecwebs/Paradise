---
title: Blog
layout: base.njk
---

<h1>Blog</h1>

<ul>
  {% for post in collections.blogs %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <small> — {{ post.date | readableDate }}</small>
    </li>
  {% endfor %}
</ul>

