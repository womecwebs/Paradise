---
layout: base.njk
---

<article class="destination">
  <h1>{{ title }}</h1>

  {% if hero_image %}
    <img src="{{ hero_image }}" alt="{{ title }}">
  {% endif %}

  <div class="content">
    {{ content | safe }}
  </div>
</article>
